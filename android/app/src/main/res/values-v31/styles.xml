<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Android 12+ replaces the starting window with the SplashScreen API,
      which will show the app icon on a themed background unless it is
      told otherwise. Both the background and the animated icon are black,
      and the icon background is black, so the splash is indistinguishable
      from the launch window: the panel stays dark through the handover
      into the Mustang pony sequence.
    -->
    <style name="LauncherTheme" parent="android:style/Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/black</item>
        <item name="android:colorBackground">@color/black</item>
        <item name="android:statusBarColor">@color/black</item>
        <item name="android:navigationBarColor">@color/black</item>
        <item name="android:windowLightStatusBar">false</item>
        <item name="android:windowLightNavigationBar">false</item>
        <item name="android:windowContentTransitions">false</item>
        <item name="android:windowAnimationStyle">@null</item>
        <item name="android:windowSplashScreenBackground">@color/black</item>
        <item name="android:windowSplashScreenAnimatedIcon">@drawable/black</item>
        <item name="android:windowSplashScreenIconBackgroundColor">@color/black</item>
        <item name="android:windowSplashScreenAnimationDuration">0</item>
    </style>
</resources>
